name: Validate redroid arm64 + Free Fire

# Compatibility check only. Starts redroid arm64, installs the APK from a draft
# release asset, launches the game, and uploads logs/screenshots.
#
# IMPORTANT: the privileged redroid container can wedge the Docker daemon, making
# any synchronous `docker logs/ps/inspect/exec` hang in uninterruptible (D) state
# that even `timeout` cannot kill. So after `docker run` we NEVER call the docker
# CLI synchronously — log capture runs in the background and all readiness /
# diagnostics go through `adb` (a separate process) and host `dmesg`.

on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: "Draft release tag containing freefire.apk"
        required: false
        default: "ff-apk-v1"
      redroid_tag:
        description: "redroid arm64 image tag"
        required: false
        default: "redroid/redroid:11.0.0-latest"
      observe_seconds:
        description: "Seconds to observe the game process after launch"
        required: false
        default: "150"

permissions:
  contents: read

env:
  ADB_TARGET: "127.0.0.1:5555"

jobs:
  validate:
    runs-on: ubuntu-22.04-arm
    timeout-minutes: 35
    steps:
      - name: Initialize artifacts
        run: |
          for f in result.txt exit-info.txt crash.log grep.log adb-devices.txt \
                   redroid-container.log android-logcat.txt host-dmesg.txt; do
            : > "$f"
          done

      - name: Host info
        timeout-minutes: 2
        run: |
          set -x
          uname -a; nproc; free -h
          echo "arch: $(dpkg --print-architecture)"

      - name: Load binder kernel module
        timeout-minutes: 5
        run: |
          set -eux
          sudo apt-get update -y
          sudo apt-get install -y adb || true
          if ! modinfo binder_linux >/dev/null 2>&1; then
            sudo apt-get install -y "linux-modules-extra-$(uname -r)"
          fi
          sudo modprobe binder_linux devices="binder,hwbinder,vndbinder"
          grep -w binder /proc/filesystems
          sudo mkdir -p /dev/binderfs
          mountpoint -q /dev/binderfs || sudo mount -t binder binder /dev/binderfs
          ls -la /dev/binderfs
          for device in binder hwbinder vndbinder; do
            test -c "/dev/binderfs/$device"
            sudo chmod 666 "/dev/binderfs/$device"
          done

      - name: Start redroid arm64
        timeout-minutes: 8
        run: |
          set -eux
          timeout --foreground --kill-after=15s 6m docker pull "${{ inputs.redroid_tag }}"
          docker image inspect "${{ inputs.redroid_tag }}" --format '{{json .Architecture}} {{json .Os}}'
          docker run -d --privileged --name redroid \
            -p 5555:5555 \
            -v /dev/binderfs/binder:/dev/binder \
            -v /dev/binderfs/hwbinder:/dev/hwbinder \
            -v /dev/binderfs/vndbinder:/dev/vndbinder \
            "${{ inputs.redroid_tag }}" \
            androidboot.use_memfd=1 \
            androidboot.redroid_width=1280 \
            androidboot.redroid_height=720 \
            androidboot.redroid_dpi=240 \
            androidboot.redroid_gpu_mode=guest
          # NENHUM docker logs aqui: -f (follow) faz o runner esperar pra sempre,
          # e o one-shot pode pendurar em D-state. Readiness/diagnostico = só adb.
          echo "redroid container iniciado; readiness via adb (zero docker CLI pos-run)"
          sleep 30

      - name: Connect ADB and wait boot
        timeout-minutes: 7
        run: |
          set -ux
          target="${ADB_TARGET}"
          adb start-server
          booted=""
          for i in $(seq 1 48); do
            timeout 5s adb connect "$target" || true
            state=$(timeout 3s adb -s "$target" get-state 2>&1 || true)
            if [ "$state" = "device" ]; then
              booted=$(timeout 4s adb -s "$target" shell getprop sys.boot_completed 2>/dev/null | tr -d '\r' || true)
            else
              booted=""
            fi
            echo "try $i state=[$state] boot_completed=[$booted]"
            [ "$booted" = "1" ] && break
            sleep 5
          done
          adb devices -l | tee adb-devices.txt
          test "$booted" = "1"
          echo "Native ABI:"
          adb -s "$target" shell getprop ro.product.cpu.abi
          adb -s "$target" shell getprop ro.product.cpu.abilist

      - name: Boot failure diagnostics (no docker CLI)
        if: failure()
        timeout-minutes: 4
        run: |
          set -ux
          target="${ADB_TARGET}"
          # adb pode estar ok mesmo com daemon docker travado (port-forward via kernel)
          timeout 5s adb connect "$target" || true
          timeout 20s adb -s "$target" logcat -b all -d > android-logcat.txt 2>&1 || true
          sudo dmesg --ctime | tail -300 > host-dmesg.txt 2>&1 || true
          echo "=== Android fatal logs ==="
          grep -iE "fatal|abort|binder|servicemanager|hwservicemanager|signal 6|SIGABRT" android-logcat.txt | tail -160 || true
          echo "=== host dmesg (tail) ==="
          tail -150 host-dmesg.txt || true

      - name: Download and install Free Fire
        timeout-minutes: 10
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -eux
          gh release download "${{ inputs.release_tag }}" \
            --repo "$GITHUB_REPOSITORY" --pattern freefire.apk --dir . --clobber
          mv -f freefire.apk ff.apk
          ls -lh ff.apk
          timeout --foreground --kill-after=15s 8m adb -s "${ADB_TARGET}" install -r ff.apk

      - name: Launch and observe (decisive test)
        timeout-minutes: 8
        run: |
          set -ux
          target="${ADB_TARGET}"
          adb -s "$target" shell monkey -p com.dts.freefireth -c android.intent.category.LAUNCHER 1
          sleep "${{ inputs.observe_seconds }}"
          echo "=== process alive? ==="
          if adb -s "$target" shell "pidof com.dts.freefireth" | grep -q .; then
            echo "RESULT=FF_ALIVE" | tee result.txt
          else
            echo "RESULT=FF_DIED" | tee result.txt
          fi
          adb -s "$target" shell dumpsys activity exit-info com.dts.freefireth > exit-info.txt 2>&1 || true
          adb -s "$target" logcat -d -b crash > crash.log 2>&1 || true
          adb -s "$target" logcat -d 2>/dev/null \
            | grep -iE "seccomp|PR_SET_SECCOMP|GGP|il2cpp|SIGABRT|SIGSEGV|FATAL|ndk_translation" \
            | tail -120 > grep.log || true
          adb -s "$target" exec-out screencap -p > screen.png 2>/dev/null || true
          ls -l screen.png || true

      - name: Summary
        if: always()
        run: |
          echo "## Validation result" >> "$GITHUB_STEP_SUMMARY"
          cat result.txt >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          echo '```' >> "$GITHUB_STEP_SUMMARY"
          grep -iE "seccomp|SIGABRT|SIGSEGV|FATAL" grep.log >> "$GITHUB_STEP_SUMMARY" 2>/dev/null \
            || echo "(no crash/seccomp signal captured)" >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: validation-results
          path: |
            result.txt
            exit-info.txt
            crash.log
            grep.log
            screen.png
            adb-devices.txt
            redroid-container.log
            android-logcat.txt
            host-dmesg.txt
